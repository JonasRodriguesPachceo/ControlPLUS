# O arquivo .env.example serve como um modelo para o .env real de dev/qa/prod
# IMPORTANTE! O arquivo .env é crucial para configuracao inicial da aplicacao, e deve estar presente
# na raiz do projeto antes de subir os containers ou rodar a aplicação localmente.

APP_NAME="Control Plus"
APP_ENV=production
APP_KEY= #Gere com 'php artisan key:generate' caso não seja gerado automaticamente
APP_DEBUG=false
APP_URL=https://app.seudominio.com # Substitua pelo seu domínio real

LOG_CHANNEL=stack
LOG_LEVEL=error # Nível mais alto para reduzir o volume de logs em produção

APP_TIMEZONE=America/Sao_Paulo
APP_LOCALE=pt_BR
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=pt_BR
# PUID e PGID são frequentemente usados em ambientes Docker, ajuste conforme sua infra
PUID=1000
PGID=1000

# ----------------------------------------------------------------------
#  Configurações do Banco de Dados (Use credenciais fortes!)
# ----------------------------------------------------------------------
DB_CONNECTION=mysql
DB_HOST=127.0.0.1 # Ou o IP/hostname do seu servidor de DB externo/gerenciado
DB_PORT=3306
DB_DATABASE=control_plus_prod
DB_USERNAME=usuario_app_prod # Nome de usuário dedicado
DB_PASSWORD=SENHA_MUITO_FORTE_DO_BANCO_PROD

# As variáveis MARIADB_ são tipicamente para o setup inicial de um contêiner DB.
# Elas não são usadas pela aplicação Laravel, mas sim pelo Docker Compose/Kubernetes.
# Se for usar, use senhas fortes aqui também:
# MARIADB_ROOT_PASSWORD=SENHA_ROOT_FORTE
# MARIADB_DATABASE=control_plus_prod
# MARIADB_USER=usuario_app_prod
# MARIADB_PASSWORD=SENHA_MUITO_FORTE_DO_BANCO_PROD

# ----------------------------------------------------------------------
# Configurações de Cache e Fila (Redis)
# ----------------------------------------------------------------------
CACHE_DRIVER=redis
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120

REDIS_HOST=127.0.0.1 # Ou o IP/hostname do seu servidor Redis
REDIS_PASSWORD=SENHA_FORTE_DO_REDIS # Use uma senha para proteger o Redis
REDIS_PORT=6379

# ----------------------------------------------------------------------
#  Configurações de E-mail (Serviço Transacional)
# ----------------------------------------------------------------------
MAIL_MAILER=smtp # Ou outro driver como 'ses', 'mailgun', etc.
MAIL_HOST=smtp.SEU_PROVEDOR.com # Ex: smtp.sendgrid.net
MAIL_PORT=587 # Ou 465, dependendo do provedor
MAIL_USERNAME=API_KEY_OU_USUARIO_SMTP
MAIL_PASSWORD=SENHA_OU_CHAVE_SECRETA_DO_SMTP
MAIL_ENCRYPTION=tls # Ou 'ssl'
MAIL_FROM_ADDRESS=noreply@seudominio.com
MAIL_FROM_NAME="${APP_NAME}"

# ----------------------------------------------------------------------
# Configurações de Frontend (Vite/Mix) e Serviços
# ----------------------------------------------------------------------
VITE_PORT=5173 # Geralmente não é usado em produção, pois os assets são compilados

# O VITE_BACKEND_URL deve apontar para o domínio público da sua API
VITE_BACKEND_URL=https://app.seudominio.com

VITE_DEV_SERVER_URL=https://app.seudominio.com # Deve ser a URL de produção

# Use um driver real para broadcasting (ex: 'redis' ou 'pusher')
BROADCAST_DRIVER=redis
FILESYSTEM_DISK=s3 # Se usar AWS S3 (ou 'local' se armazenar no servidor)
QUEUE_FAILED_DRIVER=database-uuids

# ----------------------------------------------------------------------
# Configurações de Porta (Geralmente não usadas pelo Laravel em si,
# mas sim pela sua infraestrutura, como Nginx/Proxy reverso)
# ----------------------------------------------------------------------
APP_PORT=8080
FORWARD_DB_PORT=3306 # Não forwarde portas de DB publicamente
FORWARD_REDIS_PORT=6379 # Não forwarde portas de Redis publicamente
